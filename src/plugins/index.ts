import type { Plugin } from 'payload'

// V2 keeps Payload focused on blog content. Extra plugins that create Forms,
// Redirects, Search, or other content collections are intentionally disabled.
export const plugins: Plugin[] = []
